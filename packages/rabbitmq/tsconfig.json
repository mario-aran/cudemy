{
  "extends": "@scope/tsconfig/tsconfig.node.json",
  "compilerOptions": {
    "outDir": "./dist",

    // Path aliases
    "baseUrl": ".", // Must be set for path aliases to work
    "paths": { "@/*": ["src/*"] }
  },
  "include": ["src"]
}
